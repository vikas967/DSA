 #include<stdio.h>
 int main(){
    int a=5;
    int* b=&a;
       
     printf("%d",*b);               // p se adress print hota hain 
    return 0;
 }                                                          // it's stores the adress of variable
 






// *x se use variable ke adress ka variable print hota h   *x is the sign of pointer 